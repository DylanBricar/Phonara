{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Minimal capabilities for the recording overlay window",
  "windows": ["recording_overlay"],
  "permissions": [
    "core:default"
  ]
}
